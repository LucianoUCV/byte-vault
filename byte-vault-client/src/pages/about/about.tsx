import { Box, Typography } from "@mui/material";

export const AboutSection: React.FC = () => {
  return (
    <Box py={4}>
      <Typography variant="h4">
        We are a start-up that values your data and understand your wallet
        capacity
      </Typography>
    </Box>
  );
};

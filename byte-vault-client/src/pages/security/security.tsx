import { Box, Typography } from "@mui/material";

export const SecuritySection: React.FC = () => {
  return (
    <Box py={4}>
      <Typography variant="h4">
        We use advanced encryption algorithms for every user's data
      </Typography>
    </Box>
  );
};
